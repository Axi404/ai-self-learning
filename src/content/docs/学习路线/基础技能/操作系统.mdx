---
title: Linux
---

掌握使用 Linux 操作系统，是人工智能专业研究人员的基本技能之一，但是事实上，目前在该领域中，图形化的内容已经较多，而人工智能的科研需求仅为运行代码，因此并无过多的需求。

一般来说，市面上的服务器的版本以 Ubuntu 20.04 为主，以及目前开始陆续迭代至 Ubuntu 22.04 版本，这是因为 20.04 版本的长期支持已经过期，一些常用的软件版本已经不再支持。

### 基础指令

在这里给出几个比较常用的指令。

- `cd`：切换目录
- `ls`：查看当前目录下的文件
- `pwd`：查看当前目录
- `cp`：复制文件
- `mv`：移动文件
- `rm`：删除文件
- `mkdir`：创建文件夹
- `cat`：查看文件内容

### 安装包

在 Ubuntu 以及大多数的 Linux 发行版中，使用 `apt` 来安装包，而在一些发行版中，如 Arch Linux，则使用 `pacman` 来安装包。一般来说，比如 C++ 或者 Python 等，均可以便捷地使用包管理工具来进行安装。

### Conda

Conda 是 Python 的包管理工具，其中 miniconda 是一个轻量级的包管理工具，一般推荐使用本版本。直接在 Ubuntu

在 [miniconda 的官网](https://www.anaconda.com/docs/getting-started/miniconda/install)给出了安装的教程：

```bash
mkdir -p ~/miniconda3
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh -O ~/miniconda3/miniconda.sh
bash ~/miniconda3/miniconda.sh -b -u -p ~/miniconda3
rm ~/miniconda3/miniconda.sh
source ~/miniconda3/bin/activate
conda init --all
```

同时读者或许需要掌握 conda 的常见使用方式，主要为 `create` 和 `activate` 的用法，用于包管理。

在此基础之上，使用 `python3 your_python_file.py` 即可运行脚本。